# etl-schema-drift-handling-agent
Day 11 of the 'Agentic AI in Data Engineering' series (ETL-focused) — a schema drift handling agent for the extract stage: additive changes proceed safely, but removal or type-narrowing on critical downstream-dependent columns is always quarantined, never auto-handled. Runs on a local LLM via Ollama.
